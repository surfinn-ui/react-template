---
destinationDir: "src/stores"
---
import { <%= props.pascalCaseName %>Store } from "./<%= props.pascalCaseName %>.store"

it("<%= props.pascalCaseName %>Store should be created", () => {
  const instance = <%= props.pascalCaseName %>Store.create({})

  expect(instance).toBeTruthy()
})
