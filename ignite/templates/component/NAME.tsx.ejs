---
destinationDir: src/components
patch:
  path: src/components/index.ts
  append: "export * from \"./<%= props.pascalCaseName %>\"\n"
  skip: <%= props.skipIndexFile %>
---
import { observer } from "mobx-react-lite"

export interface I<%= props.pascalCaseName %>Props {
}

/**
 * Describe your component here
 */
export const <%= props.pascalCaseName %> = observer((props: I<%= props.pascalCaseName %>Props) => {
  return (
    <>
      <%= props.camelCaseName %>
    </>
  )
})

<%= props.pascalCaseName %>.displayName = '<%= props.pascalCaseName %> Component'

