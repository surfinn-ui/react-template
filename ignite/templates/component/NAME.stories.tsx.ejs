---
destinationDir: src/components/<%= props.pascalCaseName %>
---
import { StoryFn, Meta } from '@storybook/react';
import { <%= props.pascalCaseName %>View } from './<%= props.pascalCaseName %>View';

export default {
  title: 'Components/<%= props.pascalCaseName %>View',
  component: <%= props.pascalCaseName %>View,
} as Meta<typeof <%= props.pascalCaseName %>View>;

const Template: StoryFn<typeof <%= props.pascalCaseName %>View> = (args) => (
  <<%= props.pascalCaseName %>View {...args} />
);

export const Default = Template.bind({});

Default.args = {
  
};
