---
destinationDir: src/pages/<%= props.pascalCaseName %>
---
import { StoryFn, Meta } from '@storybook/react';
import { <%= props.pascalCaseName %>PageView } from './<%= props.pascalCaseName %>PageView';

export default {
  title: 'Pages/<%= props.pascalCaseName %>Page',
  component: <%= props.pascalCaseName %>PageView,
  parameters: {
    // More on Story layout: https://storybook.js.org/docs/react/configure/story-layout
    layout: 'fullscreen',
  },
} as Meta<typeof <%= props.pascalCaseName %>PageView>;

const Template: StoryFn<typeof <%= props.pascalCaseName %>PageView> = (args) => (
  <<%= props.pascalCaseName %>PageView {...args}/>
);

export const Default = Template.bind({});

Default.args = {
};
